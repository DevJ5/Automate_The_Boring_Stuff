arr = ['cat', 'rat', 'dog']

# Negative indexes count from the end, -1 is the last index.
print("There is a %s." % arr[-1])

# Ranges are done with a colon, from:to (but not include).
print(arr[1:3])

